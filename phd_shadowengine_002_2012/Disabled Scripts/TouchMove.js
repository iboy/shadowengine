// Moves object according to finger movement on the screenvar speedX:float = 10;var speedY:float = 10;function FixedUpdate () {    if (Input.touchCount > 0 && Input.GetTouch(0).phase == TouchPhase.Moved) {            // Get movement of the finger since last frame        var touchDeltaPosition0:Vector2 = Input.GetTouch(0).deltaPosition;                // Move object across XY plane                        rigidbody.rigidbody.AddForce (touchDeltaPosition0.x * speedX, touchDeltaPosition0.y * speedY, 0);    }                  }