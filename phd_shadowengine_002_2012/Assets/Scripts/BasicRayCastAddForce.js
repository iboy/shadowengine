// Shoot a ray whenever the user taps on the screen//var particle : GameObject;var forceX:float = 80;var forceY:float = 80;function FixedUpdate () {    for (var i = 0; i < Input.touchCount; ++i) {            if (Input.touchCount > 0 && Input.GetTouch(i).phase == TouchPhase.Moved) {            // Construct a ray from the current touch coordinates            var ray = Camera.main.ScreenPointToRay (Input.GetTouch(i).position);                        if (Physics.Raycast (ray)) {                // Create a force on object if hit, using the delta of the touch / move            	var touchDeltaPosition0:Vector2 = Input.GetTouch(i).deltaPosition;                              rigidbody.AddForce (touchDeltaPosition0.x * forceX, touchDeltaPosition0.y * forceY, 0);	                        }        }   }}